{
  "name": "desktop-fushi-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "node scripts/build-wasm.mjs",
    "predev": "npm run build:wasm",
    "dev": "astro dev --host 127.0.0.1",
    "prebuild": "npm run build:wasm",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1"
  },
  "dependencies": {
    "astro": "5.16.4"
  }
}
